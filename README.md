#
A real-time chat application built using Node.js, Express.js, and Socket.IO.
It enables instant messaging between users with a live typing indicator feature.

🚀 Features
1) Real-time messaging using WebSockets
2) Typing indicator (shows when a user is typing)
3) Instant message updates without refresh
4) Multiple users can chat simultaneously
5) Lightweight and fast server using Express
